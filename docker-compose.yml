version: "3"

services:
  jessie:
    image: "mgtsai/dockerui.base-xpra:${VERSION_CODE}-jessie"
    build:
      context: "."
      dockerfile: "Dockerfile-jessie"

  xenial:
    image: "mgtsai/dockerui.base-xpra:${VERSION_CODE}-xenial"
    build:
      context: "."
      dockerfile: "Dockerfile-xenial"
